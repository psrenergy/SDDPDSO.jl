# SDDPDSO Documentation



## Installation

This package is not registered so you can `add` it using Julia's `Pkg` manager via http or ssh:
```julia
pkg> add https://github.com/psrenergy/SDDPDSO.jl.git
```